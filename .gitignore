# Compiled output — the book PDF is built by CI on tag push
# (see .github/workflows/release.yml), so it is not tracked.
*.pdf

# Keep the figure PDFs: they are build *inputs* imported by ai4paper.typ,
# not a build output.
!figures/chapter-map.pdf
!figures/foundations-map.pdf
!figures/research-definition.pdf
!figures/llm-agent-mcp.pdf
!figures/verification-cost.pdf

# Typst / editor cruft
/build/
.DS_Store
