#import "../common.typ": *

= 选题：找到值得解决的问题 (Find the Problem)

从这一章起，流水线正式开动——这里对应全局地图（@foundations:map）的第一格。
AI 在研究里的第一价值，不是直接生成「研究点子」，
而是帮我们*定位问题空间*：先把最新工作找全、读透，
再分析还有什么值得继续做。第 1 章说过 LLM 的知识停在训练截止日期，
所以这一章的一切都建立在*让 agent 实时检索*之上——
凭模型记忆做选题，等于拿一张过期地图找路。

== 本阶段研究基础

先把「选题」这件事本身说清楚，再谈 AI 怎么帮。

=== 什么是好的研究问题

第 1 章给过研究的定义：一个还没有人回答过的问题，加上能证明回答成立的证据。
落到选题上，一个值得投入几个月的问题要同时过三关：

- *有人关心*：它连着某个领域真实的痛点或好奇心，
  写 introduction 时说得出「为什么现在该解决它」。
- *没人解决*：文献里确实还没有令人满意的答案——
  这一条只能靠调研确认，不能靠感觉。
- *你做得动*：以你手头的数据、设备、时间和技能，
  能在可预期的周期内拿出证据。三关里最常被忽略的是这一关——
  问题再漂亮，做不动就只是别人的问题。

=== 文献综述的真正目的

新手常把文献综述做成*罗列*：读了三十篇，每篇写两句总结，堆成一个列表。
这份列表回答不了任何选题问题。综述的真正产出是一张*边界图*：
这个方向上，哪些问题已经被解决、被谁、解决到什么程度；
哪些问题还开着口子、为什么难。选题就是在这张图的边界上，
挑一个开着的口子切进去。
后面 AI 参与的每一步，最终都为了画出并核实这张图。

=== 贯穿示例从这里开始

从本章起，请选定一个*你自己的、小的、真实的*研究方向——
可以是课题组正在做的方向的一个侧枝，也可以是你一直好奇的一个具体现象。
它将作为*贯穿示例*走完后面所有章节：本章用它产出研究空白报告，
第 4 章围绕它设计实验，第 6 章把它写成稿子。
方向宁小勿大：「LLM 在 X 场景下的 Y 问题」比「大模型研究」走得远得多。

== 工具：apaper-mcp——让 agent 接上文献库

第 2 章已经把 #link("https://github.com/ai4paper/apaper-mcp")[`apaper-mcp`]
装进了环境，这里正式认识它。它是一个学术检索 MCP server，
把*搜论文、拿 BibTeX、下载 PDF*做成一组标准工具，
接给任何支持 MCP 的 agent（opencode、Claude Code 都行）——
agent 由此拿到了直接查询文献数据库的手，不再依赖模型记忆。

它覆盖五个来源，各有分工，合起来基本罩住一个 CS 方向的文献面：

/ arXiv: CS 与相邻领域最新工作的第一现场——多数论文正式发表前先挂在这里。
  工具支持按*类别*（如 `cs.CL`）、*日期区间*和排序方式检索，并可下载 PDF，
  是「跟踪最新进展」的主力。
/ DBLP: CS 论文的权威元数据库，按*会议 / 期刊*过滤（`venue_filter`）最好用——
  「这个方向近三年在某会发了什么」这类问题问它；还能直接带回 *BibTeX*，
  查到即引用，从源头避免第 1 章警告过的假引用。
/ Google Scholar: 覆盖面最广的兜底搜索，跨出版方、跨领域，
  用来确认「没有漏掉别的社区的相关工作」。
/ IACR ePrint: 密码学与安全方向的预印本库，做这个方向的读者的主战场。
/ CNKI（中国知网）: 中文文献的检索与下载，需要机构网络访问；
  写中文综述、查国内进展时用。

对应的工具名一望即知：`search_arxiv_papers`、`download_arxiv_paper`、
`search_dblp_papers`、`search_google_scholar_papers`、`search_iacr_papers`、
`search_cnki_papers` 等。你不需要记参数——agent 会自己填；
你要记住的是*让它用哪个来源、加什么过滤条件*。

#tip-box[
  多来源不是摆设，而是交叉验证的手段：arXiv 告诉你「最新做到哪」，
  DBLP 告诉你「正式发表的版本和出处」，Google Scholar 兜住你不知道的社区。
  一条重要相关工作，最好能在两个来源里都出现——
  只在模型嘴里出现、任何数据库都搜不到的「论文」，几乎可以断定是幻觉。
]

工具就位后，本章的主体是接下来的两步。整条流程如 @find-problem:pipeline：
AI 负责铺开候选（检索、读 PDF、分析），你负责收窄（筛选、核对）——
正是 @foundations:map 里「AI 做加法，人做减法」在选题这一格的展开。

#figure(
  image("../figures/find-problem-pipeline.pdf", width: 100%),
  caption: [
    选题两步法：五个文献来源经 `apaper-mcp` 统一接入 agent；
    第一步检索最新工作、人筛出最相关 5 篇，第二步逐篇提取空白、
    人回原文核对，最终汇成研究空白报告。浅灰格是 AI 的加法，粗框格是人的减法。
  ],
) <find-problem:pipeline>

== 第一步：自动跟踪最新工作

选题调研的第一问是：*这个方向最新已经做到哪里？*
让 agent 带着过滤条件去查，而不是泛泛地搜。一个可以直接用的提示模板：

```text
用 apaper-mcp 调研方向「<你的贯穿示例方向>」：
1. 在 arXiv 上搜近 12 个月的相关论文（date_from 限定，类别 <cs.XX>），
   按相关性给出前 10 篇：标题、作者、日期、一句话说明它做了什么；
2. 在 DBLP 上查 <目标会议/期刊> 近三年的相关论文，附 BibTeX；
3. 用 Google Scholar 交叉检查有没有遗漏的高引用相关工作；
4. 汇总成一个列表，按「与我方向的相关度」排序，并标注每一篇的来源。
```

拿到列表后，你的工作是*筛选*：把「标题像但其实无关」的划掉，
挑出真正相关的 5 篇左右进入下一步。这一步的价值在于避免两种浪费——
重复别人已经做完的事情，以及把注意力花在早已被绕过的旧问题上。

#note-box[
  按第 2 章的检查点习惯，确认 agent *真的调用了检索工具*：
  在 TUI 里能看到 `search_arxiv_papers` 之类的调用和返回结果。
  没有工具调用就冒出来的「最新论文列表」，是模型记忆——而它的记忆是过期的。
]

== 第二步：基于 PDF 提取「还值得做什么」

列表只回答「有什么」，回答不了「还缺什么」。
第二步让 agent 把筛出的最相关论文*下载下来、逐篇读进去*
（`download_arxiv_paper` 落 PDF 到本地；纯文本模型记得先
`pdftotext`，见第 2 章），然后做的不是摘要，而是*空白分析*——对每一篇回答：

+ 它声称解决了什么问题（它的 contribution 是什么）？
+ 它的方法和实验有哪些明说的限制（limitations 一节、future work 一节）？
+ 它*没说但看得出来*的边界在哪——数据规模、场景假设、没比的 baseline？

逐篇答完再横向汇总：这 5 篇合起来，把问题空间的哪一块填上了？
剩下的口子里，哪些是「没人做」，哪些是「做不动所以没人做」？
这份汇总就是前面说的边界图的初稿。

#warning-box[
  AI 给出的每一条「研究空白」，都必须*回到原文核对*后才能采信。
  两个具体的失败模式：一是*漏读*——受 context window 限制
  （第 1 章），长论文的附录和实验细节可能根本没进模型视野，
  「论文没做 X」也许只是「模型没读到 X」；二是*编造*——
  模型为了把分析写圆，给论文安上它没说过的限制。
  核对办法很朴素：让 agent 对每条空白*引用原文的具体章节和原句*，
  你翻到那一页确认。给不出出处的结论，一律降级为「待查」。
]

核对过的空白清单，再过一遍本章开头的三关：有人关心吗？
真的没人解决吗（用第一步的检索反查）？你做得动吗？
三关都过的那一两条，就是你的候选选题。

== 本章练习

#exercise[
  对你的贯穿示例方向，用 `apaper-mcp` 跑一次完整流程，
  产出一份*研究空白报告*（research-gap report），包含四部分：
  + *最相关的 5 篇论文*：标题、出处、BibTeX（来自 DBLP 或 arXiv），
    并注明每一篇是从哪个来源、用什么查询找到的；
  + *各自解决了什么*：每篇一段，写清它的 contribution 和明说的限制；
  + *还有哪些未解决的问题*：横向汇总出的空白清单，
    每条附原文出处（论文 + 章节），没有出处的标注「待查」；
  + *你打算切哪一个*：从清单里选一条，用三关各写一句话论证——
    为什么有人关心、为什么还没人解决、为什么你做得动。
  这份报告不是一次性作业：第 4 章的实验计划、第 6 章的
  introduction 和 related work，都会直接从它出发。
]
