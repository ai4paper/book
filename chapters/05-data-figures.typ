#import "../common.typ": *

= 数据与图表 (Data, Figures, and Tables)

上一章结束时，你手里有一批经过逐行 review、能一键复现的实验产出：
结果 CSV、运行日志、若干模型输出。它们是证据，但还不是论文——
审稿人不读 CSV，读的是图和表。本章讲把数据变成图表的两段路：
先让 agent 替你*处理数据*——清洗、汇总、透视，全程不必自己写代码，
但要产出*可以重跑的脚本*；再用「提示 → 代码 → 渲染 → 迭代」的循环
做出*出版质量*的图。对应 @foundations:map 的「数据与图表」一列：
AI 生成脚本与图，人评估质量、核对每一个数字。

== 本阶段研究基础

=== 一张好的论文图的标准

第 1 章说过审稿人的读法是跳读：abstract、图表，然后才是正文。
图表是论文里被读得最认真的部分，好图的标准就从这位读者出发：

- *一眼看出结论*：一张图传达一个明确的 message——
  「我们的方法在所有负载下都更快」。读者不该需要正文的解释
  才能明白图想说什么；
- *信息自成一体*：坐标轴有名字有单位，图例完整，
  图注（caption）单独读也能懂——审稿人常常只读图和 caption；
- *印刷友好*：矢量格式（PDF/SVG），灰度打印后线条与填充仍能区分，
  图内文字和正文字号一致——被缩小到看不清的坐标轴，
  是审稿意见里的常客。

=== 常见错误

对着标准，错误就容易列了：截图或位图，放大后一片模糊；
配色在黑白打印或色盲读者眼里无法区分；
图里的数字和正文、表格对不上——最后一条最伤，
因为它动摇的是审稿人对*整篇论文数据*的信任。
这些错误的共同根源，往往是图表被「手工」维护：
改了数据忘了改图，调了图忘了同步正文。本章的解法归结成一句话：
*数据处理和图表都用代码生成，代码由 agent 写，产物由人验证。*

== 数据处理：不写代码，但要留下脚本

实验做完，原始数据很少能直接用：多个 seed 的结果要汇成
mean ± std，不同配置的日志要对齐成一张表，没跑完的 run 要有交代。
这些是典型的脚本活——现在可以全程用自然语言指挥 agent 完成。

关键的原则只有一条：*让 agent 生成可以重跑的脚本，
而不是替你做一次性的手工操作*。「帮我把这个 CSV 里的异常行删掉」
和「写一个清洗脚本，规则如下」表面上产出一样，分岔在三周后：
修回时补了两组实验、数据更新了，前者要凭记忆把手工操作重来一遍，
后者一条命令重建全部产物。脚本本身就是可复现性的一部分——
第 4 章的「一键复现」，在这里延伸到数据处理。

一个可以直接改用的提示模板：

```text
目录 results/raw/ 下是实验原始输出（每个 run 一个 CSV，
文件名含方法名和 seed，列为 <说明你的列>）。写一个汇总脚本
scripts/aggregate.py：
1. 读入全部 CSV，剔除未跑完的 run（行数不足 <N> 视为未跑完），
   剔除了哪些文件要在输出里列出来；
2. 按「方法 × 数据集」汇总 <指标列>，mean 和 std 各存一列数值，
   不做四舍五入（给论文表格看的「mean ± std」格式化版本另行生成）；
3. 汇总表写到 tables/summary.csv，不许改动 results/raw/ 下任何文件；
4. 告诉我怎么重跑，以及每一步怎么检查。
```

模板里藏着几条通用规矩：*原始数据只读*——处理产物一律写到别的目录，
出错永远可以从头再来；*异常处理要显式*——剔除了什么、为什么，
写进输出，不许静默丢数据；*产物路径固定*——下游画图脚本
直接读 `tables/`，接成一条「数据 → 表 → 图」的链。

#note-box[
  验证这一步的办法很朴素：*抽查加重跑*。从汇总表里挑两三个数字，
  用原始 CSV 手工算一遍对照；再删掉 `tables/` 整个重跑，
  确认产物一致。agent 在数据处理上最常见的错误不是算错，
  而是*理解错*——把你说的「剔除异常」理解成了另一种规则。
  抽查对上了，理解才算对上。
]

== 图表生成：提示 → 代码 → 渲染 → 迭代

=== 生成代码，而不是像素

让 AI 直接「画一张图」（输出一张位图）对论文毫无用处：改不了、
进不了版本库、风格不可复用。正确的姿势是让它*生成绘图库的代码*——
数据图用 matplotlib 这类绘图库，方法示意图用 LaTeX 的 TikZ 或
Typst 的 CeTZ（本书的插图全部由 CeTZ 画成）。代码是文本：
可以 diff、可以进 git、可以在下一篇论文里复用同一套风格，
数据更新时重新跑一遍即可——上一节那条「数据 → 表 → 图」的链，
终点就落在绘图代码上。

=== 循环：每个问题都是提示里缺的一条约束

第一版生成的图几乎不会直接达标。别急着上手改代码——
按 @data-figures:loop 的循环走：

+ *Generate*：用结构化的提示让 AI 生成绘图代码——说清数据来源、
  图型、风格约束（字号、配色、尺寸）；
+ *Evaluate*：编译渲染出预览，对照前面「好图的标准」检查；
+ *Find issues*：把问题点名——字号比正文小、图例遮住曲线、
  超出栏宽、两条线灰度打印无法区分；
+ *Update prompt*：关键一步——把问题*写回提示*，变成新的约束或
  反模式（「图例放图外右侧」「用线型区分曲线，不依赖颜色」），
  而不是只在代码里打补丁；
+ *Iterate*：用更新后的提示重新生成，直到 camera-ready。

#figure(
  image("../figures/figure-loop.pdf", width: 100%),
  // caption stays on one source line: line breaks inside captions render as spaces
  caption: [画图循环：结构化提示驱动 AI 生成绘图代码，渲染出预览后由人对照好图标准评估；不达标的问题写回提示、再生成，达标即得 camera-ready 图。循环收敛后，那份积累了约束与反模式的提示可以固化为 skill 复用。],
) <data-figures:loop>

问题写回提示而不是只改代码，是因为收益差一个量级：
改代码只修好这一张图，改提示让*以后每一张图*都不再犯这个错。
循环收敛时的产物有两个：一张达标的图，和一份固化了你风格标准的提示——
后者比前者值钱。当一份提示被打磨得足够好，
它就值得升级成第 2 章说的 *skill*：一包可复用的专门知识。
下一节先把循环完整跑一遍，再介绍一个现成的画图 skill。

== 数据图：把循环跑一遍

以上一节汇总出的 `tables/summary.csv` 为例。第一轮的提示——
注意它已经把好图标准写成了约束：

```text
读 tables/summary.csv，写一个画图脚本 figures/latency.py：
- 图型：分组柱状图，x 轴为数据集，每组三根柱对应三个方法，
  误差棒用 std；
- 风格：输出 PDF，尺寸 8.4cm × 5.5cm（单栏），字号 9pt 与正文一致，
  灰度配色，用填充纹理区分方法，不依赖颜色；
- 坐标轴：y 轴标注指标名和单位；
- 数据一律从 CSV 读，不许把任何数字硬编码进脚本。
```

渲染出来一看（*Evaluate*），两个问题（*Find issues*）：
图例压住了最高的柱子；y 轴没有从 0 开始，柱子间的差距在视觉上
被夸大了。把它们写回提示（*Update prompt*），追加两条约束：

```text
- 图例放在图外上方，横排；
- y 轴从 0 开始——截断坐标轴夸大差距，属于反模式。
```

第二轮重新生成，达标。两轮下来你多了一份带六条约束的画图提示，
下一张图从它出发，大概率一轮就过。练习会要求你保留这份迭代记录——
它是这一章真正的资产。

== 示意图：creating-figures skill

方法示意图（架构图、流程框图）没有数据，考的是布局与风格的功夫，
恰好是「提示固化成 skill」最成熟的例子。本书配套的
#link("https://github.com/ai4paper/apaper-plugin")[`apaper-plugin`]
里就打包了一个 `creating-figures` skill——把上面循环里
你本来要自己积累的约束和反模式，预先写好了。

=== 安装 apaper-plugin

这个插件打包了三样东西：`creating-figures`（本节）、
`writing`（第 6 章）两个 skill，加上第 2 章装过的 `apaper-mcp`。
opencode 用 #link("https://github.com/kdcokenny/ocx")[`ocx`] 安装：

```bash
ocx init            # 项目里第一次用 ocx 时执行
ocx registry add https://ai4paper.github.io/apaper-plugin --name apaper
ocx add apaper/creating-figures    # 只装画图 skill
ocx add apaper/apaper              # 或：两个 skill + MCP 全装
```

Claude Code 则用插件市场，两条命令：

```text
/plugin marketplace add ai4paper/apaper-plugin
/plugin install apaper-plugin@apaper
```

#note-box[
  `apaper-mcp` 在第 2 章已经配置过，重复注册没有影响：
  opencode 里单装 skill 不会碰你的 MCP 配置，Claude Code 装插件后
  自带的 server 与你手工配的指向同一个包。另外 Claude Code 里
  插件的 skill 带命名空间，显式调用要写全名
  `/apaper-plugin:creating-figures`；正文提示里直接说
  「用 creating-figures skill」，两个 runtime 都能对上。
]

=== 它替你固化了什么

打开 skill 文件能看到它就是一份结构化提示——ocx 装完后在
`.opencode/skills/creating-figures/`，或直接看仓库里的
`skills/creating-figures/`——核心几条：

- *后端由论文格式决定*：宿主论文是 `.tex` 就生成 TikZ，
  是 `.typ` 就生成 CeTZ，从不混用——skill 会自己检测；
- *灰度五档*：文字最黑、边框次之、填充最浅，
  打印友好是默认而不是选项；
- *图内文字等于正文字号*：字号从宿主论文里取，
  且插入时不允许缩放——`width: 80%` 这类写法会把字压小，
  图太宽就收紧图内布局，而不是把图缩小
  （本书插图按版心宽度设计、以原宽插入；期刊双栏的空间紧得多，
  这条规矩更要守住）；
- *自验证循环*：skill 要求 agent 生成后自己编译、渲染 PNG 预览、
  对照清单检查（文字重叠、箭头方向、留白、灰度对比），
  修好再交付。

最后一条值得停一下：@data-figures:loop 里「渲染 → 评估」那两步，
skill 教 agent 自己先走若干轮，你拿到的已经是它自检过的版本。
使用时一句话即可：

```text
用 creating-figures skill 画我的方法示意图：输入经 <模块 A> 与
<模块 B> 并行处理后在 <模块 C> 汇合，虚线框标出本文新增的部分。
论文主文件是 paper.typ。
```

#tip-box[
  skill 的自检替你把住的是*形式关*：重叠、字号、配色这类
  清单能写下来的标准。*内容关*仍然只有你能把——模块关系画对了吗、
  箭头方向符合数据流吗、这张图放进论文讲的 story 对吗。
  人的评估从「第一轮」后移成了「验收」，但不能取消。
]

== 验证原则

图表是审稿人核对最细的地方，本章的「人 · 验证」一格有四条：

- *图里的数字可追溯*：画图脚本只从 `tables/` 读数据，
  禁止硬编码数组——图文不一致的头号来源就是「图里的数字是手敲的」。
  抽查办法：挑图上一个点，回 CSV 里找到它。
- *一键重建*：删掉 `tables/` 和 `figures/` 的产物，
  从原始数据两条命令重建，和提交版本一致。提交前跑一次，
  「改了数据忘了改图」在这里现形。
- *打印测试*：灰度预览一遍，确认曲线和填充仍能区分；
  再按论文实际栏宽预览一遍、缩小到 50% 再看一眼，
  确认文字仍然可读。
- *caption 自成一体*：写图注时想象一位只看图的审稿人——
  图注要说清「这是什么、怎么读、结论是什么」。
  图表怎么在正文里被引用和描述，是下一章写作的内容。

== 本章练习

#exercise[
  用第 4 章实验产出的数据（或任一份你手头的多次运行结果），
  把本章流程完整走一遍：
  + *数据*：让 agent 写清洗汇总脚本，产出 `tables/summary.csv`；
    从表里抽三个数字回原始数据核对；
  + *数据图*：按画图循环产出一张 camera-ready 图，
    至少迭代两轮，保留每一轮的提示和发现的问题——
    这份记录就是你自己的画图提示资产；
  + *示意图*：用 `creating-figures` skill 画一张贯穿示例的
    方法框图，验收内容正确性（模块、箭头、分组）；
  + *重建*：删掉 `tables/` 与 `figures/` 的产物一键重建，
    确认与你要提交的版本一致。
  交付：脚本 + 图源文件 + 最终图 + 提示迭代记录。
  下一章，这些图和表进入稿子。
]
