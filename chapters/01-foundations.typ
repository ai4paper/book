#import "../common.typ": *

= 基础：研究 + LLM (Foundations)

这一章同时讲两件事：研究是怎么回事，LLM 是怎样一种工具。
全书关心的是「AI 能不能切进研究流程的具体环节」——不懂研究，就说不清该切进哪里；
不懂 LLM，就分不清它哪里可靠、哪里只是像模像样。
最后把两块基础叠成一张贯穿全书的*全局地图*（@foundations:map）：
每个环节上 AI 做什么、人做什么、什么必须由人验证。

== 研究基础（科研入门）

发表过论文的读者可以把这一节当作术语对齐，快速略读；
科研新手请从这里开始——后面各章讨论「AI 在这个环节帮什么」时，
默认你知道「这个环节本来是干什么的」。

=== 研究到底是什么

数据库课的作业要求实现一个 B+ 树：题目是老师出的，
答案早已存在，评分标准写在说明里。
换个场景：你注意到某类查询在真实负载下总比理论预期慢，
没有人解释过为什么，也没有评分标准告诉你「答对了」——
你必须自己提出解释，再用实验证明它站得住。前者是作业，后者是研究。

#definition(title: [研究 · research])[
  研究 = 一个*还没有人回答过的问题* + 能证明你的回答成立的*证据*。
  两个成分缺一不可：只有新问题而没有证据，是观点；
  只有证据而问题早已被回答，是练习。
]

#figure(
  image("../figures/research-definition.pdf", width: 68%),
  caption: [研究 = 新问题 + 证据：两个成分都齐了才是研究；缺证据落回观点，缺新意落回练习。],
)

回到 B+ 树那道作业，两个成分的差别就落到了实处。
作业有标准答案，研究的价值恰恰在于「新」——
重复别人做完的事，做得再漂亮也不构成研究。
作业只需说服打分的老师，研究要说服的是一群以挑毛病为职责的同行（peer），
所以还要拿出经得起审视的证据：实验、证明、数据。

=== 研究的完整生命周期

一项研究从无到有，会经过一条相对固定的流水线：

#align(center)[*选题（含文献调研）→ 实验 → 数据与图表 → 写作 → 投稿 → 修回 → 发表*]

每个阶段都有明确的*产出物*和「做完了」的标准：

- *选题与文献*：一个值得回答的问题，加上一张「已解决／未解决」的边界图——
  而不是一份读过的论文清单。标准：一句话说清它为什么还没被解决。
- *实验*：支撑结论的证据——有 baseline、有对照。标准：*可复现*，
  而非「曾经跑出过一次好数字」。
- *数据与图表*：能直接放进论文的图和表。标准：一眼能看出要传达的结论。
- *写作*：一份按论文文体组织的稿子。标准：经得起审稿人跳读。
- *投稿*：完整的投稿包——cover letter、图源文件、合规检查。
- *修回*：修改稿，加上逐条回应审稿意见的回复信。
- *发表*：camera-ready 终稿和让工作被看见的传播材料。

第 3–9 章沿流水线逐段展开——文献调研并入选题（第 3 章），
数据与图表单独成章（第 5 章），七个阶段正好对应七章。
在沿这条流水线往下走之前，先拆掉两个常见误区：一是*以为写作最后才开始*——
逻辑漏洞往往是往纸上写时才暴露的，老手边做实验边搭论文骨架；
二是*以为投稿之后就结束了*——多数论文的第一个结果是修回（revision）而非录用，
评审与修改常和写初稿一样耗时（所以第 8 章整章讲修回）。

=== 论文这种文体

论文投出去之后会发生什么？编辑把它转给两三位审稿人（reviewer）——
同领域的研究者，无偿评审，手里还有自己的实验和 deadline。
所以他不会逐字精读，而是跳读：abstract、图表，再有针对性地读方法细节找漏洞。
读完，他要回答三个问题：*新*吗（novelty）？*对*吗（correctness）？
讲*清楚*了吗（clarity）？

论文的固定结构（abstract / introduction / methods / results / conclusion），
正是为这位又忙又挑剔的读者设计的：每一节回答一个固定问题；
它不是束缚，而是一种*接口约定*——审稿人知道去哪个位置找哪类信息。

这里出现本书反复使用的一个词：*contribution*（贡献）——
一句可以被检验的声明，加上正文里支撑它的证据。
「我们提出方法 X，在场景 Y 下比现有方法快 Z 倍（第 5 节实验）」是一条
contribution；「我们研究了问题 X」不是——它只描述了动作，没有说出增量。
两个朴素的测试：删掉它，论文的价值是否明显下降；能否用一句话讲给同行听。
审稿人评审，就是逐条检验这些声明。

== LLM 基础

现在换到另一边。本节只回答研究者需要的三件事：
怎么工作、边界在哪里、规矩是什么。先给出三个术语，再逐个展开。

#definition(title: [LLM · agent · MCP])[
  / LLM: 大语言模型（large language model）：在海量文本上训练、
    「给定上下文，预测下一个词」的模型；擅长语言、模式与代码。
  / Agent: LLM + 工具调用 + 循环：能检索文献、读 PDF、执行代码，
    观察结果后决定下一步，而不只是生成回答。
  / MCP: Model Context Protocol：agent 与外部工具之间的标准协议——
    工具实现一次，任何支持 MCP 的 agent 都能接入。
]

=== 用研究者的语言理解 LLM、agent 和 MCP

先看 LLM。主流实现都基于 Transformer 架构@vaswani2017attention；
当模型和数据规模足够大，「预测下一个词」表现出远超「补全句子」的行为——
不需要针对任务重新训练，只要在提示里给出任务描述和少量示例，
就能举一反三地完成新任务@brown2020language。
还有一个先天机制要一并记住——*context window*（上下文窗口）：LLM 没有持久记忆，
只能「看到」装进窗口的内容，而窗口再大也有限。

对研究者来说，它是一台*语言与模式的引擎*：总结、改写、写代码都又快又像样。
但它输出的本质是*统计上最像正确答案的文本*，而不是查出来的事实；
「像正确」和「正确」的缝隙，是下一小节的主题。

只会对话的 LLM 离研究工作流还有距离：研究任务往往需要
*查资料、读文件、跑代码、看结果、再决定下一步*。
把 LLM 放进这样一个循环，让它调用工具、根据结果继续行动，就是 *agent*。
例如你对 agent 说「读这篇 PDF，总结它的方法部分」，
它实际做的大致是——调用 PDF 解析工具把文件转成文本；
发现全文超出 context window，先扫目录、分段读取；
定位到方法章节读入相关段落；最后生成总结。
每一步调用了什么工具、读进了哪些内容，好的 agent 界面都能查到——
第 2 章搭环境的检查点专门有这一条，原因就在这里：
*验证 AI 的产出，常常要从验证它的过程开始*——
一份「没读方法章节就写出来的方法总结」，再流畅也不可信。

agent 的工具从哪里来？*MCP* 是一个开放协议，
规定外部工具——文献库、编译器、数据源——如何统一接入 agent：
工具按协议实现一次，任何支持 MCP 的 agent 都能用。安装配置见第 2 章；现在只需记住分层直觉：
LLM 是引擎，agent 是围绕引擎的执行循环，MCP 是给循环接上外部世界的插座。

#figure(
  image("../figures/llm-agent-mcp.pdf", width: 82%),
  caption: [分层直觉：LLM 是「预测下一个词」的引擎，agent 是围绕它的执行循环，MCP 是让循环接上外部工具的插座。],
)

=== LLM 的局限

能力清单必须和局限一起读，否则容易高估 AI。

第一是*幻觉*（hallucination）：LLM 会以完全一致的流畅和自信输出
编造的内容@ji2023survey。这不是偶发故障，
而是「预测下一个词」的内生副作用——还是那道「像正确」与「正确」之间的缝隙：
模型生成「看起来最像真的」的文本，而「像真的」不等于「是真的」。
对研究者来说，最危险的形态是*假引用*。

#warning-box[
  假引用是研究场景下最致命的幻觉：模型给出的参考文献作者、标题、期刊、
  年份俱全，但那篇论文*根本不存在*——或者存在，内容却对不上。
  这条引用一旦混进 `references.bib`，会安然通过拼写检查、正常参与编译，
  一路活到某个审稿人顺手去查它的那一刻；而审稿人一旦抓到一处编造，
  会开始怀疑整篇论文。所以 AI 给出的每条文献，进入论文前都必须核对：
  原文存在，且真的说了被声称的内容。
]

第二是*知识过时*：模型只见过训练截止日期（knowledge cutoff）之前的数据，
之后的工作一无所知——而且往往不承认，而是给出过时或编造的回答，
这对依赖「最新工作做到哪里」的选题尤其致命。
缓解办法是 agent 加检索工具，让它实时去查、不依赖记忆——
但检索只缓解「过时」，模型对材料的解读仍可能出错，核对的责任没有消失。

第三是*上下文窗口的硬边界*：超出窗口的内容，模型是真的「看不见」，
读长 PDF、大代码库时表现为遗漏细节或前后不一致；
对策是把大任务拆小，把重要的中间结论显式写下来，需要时再喂回去。

能力和局限放在一起，就能回答前言里那个问题——哪里真的省时间，
哪里只是看起来很聪明。实用的判据是*验证成本*：
AI 真正省时间的地方，是产出容易验证的地方——代码可以跑测试，
引用可以查文献库，格式可以编译检查，改写可以逐句对读；
危险的地方是产出难以验证的地方——「这个方向值得做」「这个实验公平」，
验证这些靠的是你自己的专业判断。

由此得到全书反复出现的分工原则：
*AI 做加法，人做减法*——AI 快速生成候选，人筛选并验证；
最终为结论负责的是你，不是模型。

#figure(
  image("../figures/verification-cost.pdf", width: 92%),
  caption: [用*验证成本*判断 AI 用在哪里：产出越容易验证，AI 越省时间；越难验证，越要靠人的专业判断。],
)

=== 学术诚信：AI 时代的规矩

局限说的是「AI 可能出错」，诚信说的是「就算 AI 没出错，有些事也不能做」。
有几条几乎已是共识：
*AI 不能署名为作者*——作者身份意味着能为正确性和原创性负责，
模型无法承担责任，这正是「必须由人验证」的制度镜像；
*实质性的 AI 生成内容需要披露*——生成成段正文或实质参与分析，
多数出版方要求说明，单纯的语言润色一般不在此列；
*编造就是学术不端，与工具无关*——用 AI 编造数据、插入不存在的引用，
性质与手工造假完全相同，「这是 AI 写的」不构成任何辩护。

可以据此画一条粗线：润色语言、写实验代码、整理文献，是普遍接受的用法；
一旦越过前面三条——冒名作者、隐瞒实质性使用、编造内容——就落到普遍禁止的一侧。
真正难办的是中间的灰色地带，各期刊划法不一，而且还在变化——所以请养成习惯：
*投稿前到目标期刊官网查一次当前的 AI 使用政策*，
并以官网当时的原文为准——不要依赖任何转述，包括本书。
最后一条容易被忽略：*未发表的稿件是保密材料*——无论是你自己未投出的手稿，
还是你作为审稿人拿到的稿件，上传给任何外部 AI 服务前，先确认被允许。

== 全局地图：LLM 如何与研究协同 <foundations:map>

两块基础都齐了，叠在一起就是全书最重要的一张图：
每个环节上 AI 做什么、人做什么、什么必须由人验证。
（第 2 章不在图里：搭建环境是流水线开动的前提，不是流水线上的一段。）

#figure(
  image("../figures/foundations-map.pdf", width: 100%),
  caption: [
    研究流程中 AI 与人的分工地图：横轴是七个阶段，三条泳道分别是
    AI 生成、人筛选、人验证；第 3–9 章各放大其中一列。
  ],
)

这张图值得横竖两个方向看。*横着看*每一条泳道：「AI · 加法」一行全是*加法*——
检索、生成、起草，把候选项快速铺开；「人 · 判断」与「人 · 验证」两行全是*判断与把关*——
什么值得做、投到哪里、如何答辩，要么依赖对领域的品味，要么需要有人为后果负责。
*竖着看*每一个阶段：那一列下面两格从不为空——这张地图描述的是协同，不是替代。
最底下「人 · 验证」一行也没有空格：核对原文、review diff、复现结果、查官网，
正是「AI 做加法，人做减法」在整条流水线上的展开；
每一章的练习，练的就是那一格的检查动作。

== 本章练习

从第 3 章起，你将选定一个自己的小型真实研究方向，用它贯穿全书；
在那之前，本章练习只需要一张纸。

#exercise[
  + 用自己的话写下：你所在（或最感兴趣）方向的研究生命周期里，
    每个阶段的*产出物*和「做完了」的标准。
    写不出来的地方标注出来——那正是值得先补的部分。
  + 对照分工地图（@foundations:map），标出你目前*最卡*的阶段——
    它决定你可以先精读哪一章；本书按流水线顺序组织，但不必按顺序读。
]

写完这两条，你就有了一张私人版的地图。
下一章先把工具环境搭起来，让 agent 真的能检索论文、读 PDF；
从第 3 章起，带着自己的研究问题上路。
