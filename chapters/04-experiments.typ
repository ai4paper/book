#import "../common.typ": *

= 实验：让 agent 并行执行 (Run the Experiments)

上一章结束时，你手里有一份研究空白报告和一个选定的问题。
下一步不是继续和模型聊天，而是*执行*：写实现、跑验证、拿到证据。
本章讲如何把实验流程从「单线程手工推进」变成*多 agent 协同推进*——
先用计划把大问题拆成小任务，再让多个 agent 并行做，
人退到监工的位置：看板式地检查、筛选和接管。

== 本阶段研究基础

=== 实验是证据，不是「跑个程序」

实验在研究里的角色，是为你的 claim 提供证据。
第 1 章说过，一条 contribution 是「可以被检验的声明 + 支撑它的证据」——
实验就是那个证据的主要来源。所以设计实验的起点不是「我能跑什么」，
而是「我要证明什么」：每个实验都应当能指回一条具体的 claim；
指不回去的实验，跑得再多也进不了论文。

=== 什么叫一个可信的实验

审稿人看实验，检查的是三件事：

- *baseline*：和现有方法比了吗？比的是不是该领域公认的强 baseline，
  而不是专挑弱的打？
- *对照*：变量隔离了吗？你声称改进来自方法 X，
  就要有「只差 X」的对照组，否则改进可能来自调参或数据。
- *可复现*：换台机器、换个人，按你的脚本能跑出同样的数字吗？
  「曾经跑出过一次好数字」不算结果。

=== 先写实验计划，再动手

动手前用一页纸回答三个问题：*要验证什么*（对应哪条 claim）、
*和谁比*（baseline 与对照怎么设）、*怎么算成功*（指标和判据是什么）。
这页纸在传统流程里是给自己看的备忘；在 agent 流程里，
它升级成了*给 agent 的任务书*——下一节整个建立在它之上。

== 第一步：用 plan 模式把大问题拆小

「把我的方法实现出来并和三个 baseline 比较」是一个 agent 一口吃不下的任务：
上下文装不下，中途出错也无从定位。正确的做法是先*把大问题拆成小问题*，
再逐个执行。拆解本身也可以让 agent 来做——但要用受约束的方式。

=== superpowers：一套现成的计划与执行 skill

#link("https://github.com/obra/superpowers")[`superpowers`]
是一套开源的 agent skill 集合（skill 是什么见第 2 章的复用层），
其中与本章直接相关的有三个：

/ `writing-plans`: 计划模式的核心。给它你的实验任务书，
  它产出一份*实现计划*文档：把任务拆成一个个 2–5 分钟粒度的小步骤，
  每步写明确切的文件路径、完整的代码、要运行的命令和预期输出。
  它的规矩对研究代码尤其有价值——*不许有占位符*：
  「TBD」「稍后实现」「补充适当的错误处理」这类步骤被明确定义为计划失败。
/ `executing-plans`: 拿着计划逐任务执行，在检查点停下来等你 review。
/ `subagent-driven-development`: 每个任务派发一个全新的 subagent 执行，
  任务之间过 review——这是通往下一节「并行」的桥。

用法很直接：把 skill 装进 runtime（照仓库说明放进 skill 目录），
然后对 agent 说：

```text
用 writing-plans skill 给下面的实验写一份实现计划：
<粘贴你的一页纸实验任务书：要验证什么、和谁比、怎么算成功>
```

产出的计划是一个 Markdown 文件，任务带复选框。*先读计划再放行*：
数据路径对不对、baseline 配置和论文一致吗、评测指标算对了吗——
在计划里改一行字，比在跑完的实验里追一个错便宜一百倍。

#note-box[
  Claude Code 自带一个原生的 *plan mode*（`Shift+Tab` 切换）：
  该模式下 agent 只读代码、只做调研，产出一份行动计划，
  经你批准后才开始改动。它和 `writing-plans` 解决同一个问题——
  「先拆解、先过目、再执行」；区别在于 skill 版把计划落成了
  可以逐条追踪、可以交给其他 agent 执行的文档。小任务用 plan mode 就够，
  跨多天的实验用计划文档。
]

=== 为什么拆小对实验特别重要

第 1 章讲过 context window 的硬边界：任务越大，agent 越容易漏细节、前后不一致。
拆成小任务后，每个任务自带完整上下文、单独可验证——
一个任务出错，重跑那一个就行。更重要的是研究上的理由：
实验的可信来自*每一步都被检查过*，而只有足够小的步骤，人才检查得过来。

== 第二步：多 agent 并行，加速实验

计划拆好之后会发现：很多任务互相*不依赖*——
三个 baseline 的复现、两个数据集的预处理、消融实验的各个配置，
彼此不共享状态。这正是并行的空间：与其让一个 agent 排队做完，
不如同时派出多个 agent，各做各的。

=== Claude Code 的 subagent 工作流

Claude Code（opencode 同理）可以在一个会话里派发 *subagent*：
主 agent 负责协调，把独立任务分发给多个子 agent 并行执行，
每个子 agent 有自己独立的上下文，互不污染。你只需要在提示里说清并行意图：

```text
以下三个任务互相独立，请并行派发三个 subagent 分别完成：
1. 复现 baseline A：<仓库、配置、数据路径、预期指标范围>
2. 复现 baseline B：<同上>
3. 实现数据预处理脚本：<输入输出格式、字段定义>
每个 subagent 完成后返回：做了什么、结果数字、遇到的问题。
```

`superpowers` 里的 `dispatching-parallel-agents` skill
把这套做法固化成了规矩，其中最重要的几条：

- *一个 agent 一个独立问题域*：任务之间不共享文件、不共享状态才能并行；
  会互相写同一个结果目录的任务，串行做。
- *每个 agent 的提示自包含*：范围（做哪个任务）、上下文（需要的路径和配置全部给出，
  它看不到你的会话历史）、约束（「不许改动其他代码」）、
  预期输出（「返回结果数字和复现命令」）。
- *回收时逐个 review 再整合*：读每个 agent 的总结，检查改动是否冲突，
  最后跑一次全量验证——agent 会犯系统性错误，并行只是加速，不豁免检查。

#tip-box[
  并行的收益边界要想清楚：*相互依赖*的任务（先有预处理才有训练）并行不了；
  *探索性*的任务（还不知道问题出在哪）也不适合并行——先单线程摸清楚，
  拆解清晰之后再撒 agent。经验法则：计划文档里能画成互不相连的任务，才并行。
]

两步合起来，就是本章的完整工作流（@experiments:workflow）：
任务书拆成计划，计划里独立的任务并行派发，
所有产出汇到人手里 review、复现，才成为可信的证据。
图上方那块「看板」，就是下一节的主角。

#figure(
  image("../figures/experiment-workflow.pdf", width: 100%),
  caption: [
    实验工作流：一页纸任务书 → `writing-plans` 拆成小任务计划（人先读再放行）→
    互不依赖的任务并行派发 subagent → 人 review 整合 → 一键复现 → 可信证据；
    ikanban 看板悬在并行段上方，集中监督进度、权限与 diff。
  ],
) <experiments:workflow>

== 工具：ikanban——多 agent 的看板

一旦同时跑着三五个 agent，终端窗口就不够用了：
你需要一个地方*集中看*谁在跑、跑到哪、改了什么。
#link("https://github.com/isomoes/ikanban")[`ikanban`]
就是为此而生的 *multi-agent coding workspace*——
一个驱动 OpenCode 的 Web 界面，把会话管理、diff 审查和多项目导航收进一处：

- *Home board*：看板式地列出所有 session 的状态——哪个在跑、
  哪个在等你审批权限、哪个已经完成待 review，实验整体进度一屏看全；
- *Review flow*：直接查看每个 agent 的改动和 diff，
  不用切回终端就能完成「合入前人工 review」这道工序；
- *Task graph*：展示 parent / child agent 的结构——
  上一节派发出去的 subagent 各自在做什么、做到哪一步，一目了然。

=== 远程模式：实验跑在服务器上，人在浏览器里监工

实验的现实是：代码跑在实验室的 GPU 工作站或远程服务器上，
而你人在笔记本前——甚至在路上。ikanban 是纯 Web 前端，
可以连接*任意机器*上的 OpenCode server，这让「远程驱动 agent」变成一条命令的事。

在跑实验的远程机器上启动 OpenCode server，并对托管页面开启 CORS：

```bash
opencode serve --port <PORT> --cors https://isomoes.github.io
```

然后在任何设备的浏览器里打开托管应用
#link("https://isomoes.github.io/ikanban")[`https://isomoes.github.io/ikanban`]，
在设置中添加服务器地址（如 `http://<服务器>:<PORT>`，
支持 Basic 认证和多服务器切换）。不想用托管页面，也可以在本地自己起前端：

```bash
npx ikanban-web@latest                                     # 本地 3000 端口
OPENCODE_URL=http://myserver:4096 npx ikanban-web@latest   # 指向远程 server
```

#figure(
  image("../figures/ikanban-remote.pdf", width: 72%),
  caption: [
    ikanban 远程模式：浏览器里的看板通过 HTTP 连接远程机器上的
    `opencode serve`，人从任何设备发提示、审批权限、review diff，
    实验则由服务器上的多个 agent session 并行推进。
  ],
) <experiments:remote>

典型场景（@experiments:remote）正好覆盖实验阶段的痛点：

- 实验在高性能工作站上由 agent 推进，你从笔记本浏览器*远程创建会话、
  发提示词、审批权限请求、review diff*；
- 从 Windows 访问跑在 WSL 里的 OpenCode，或通过 SSH 隧道够到机房的机器；
- 同时监督*多个项目、多个并行 agent*：baseline 复现和消融实验各自在跑，
  你在同一块看板上巡视，谁停在权限审批上就点掉，谁的 diff 到了就审。

#note-box[
  远程模式改变的是*人的位置*，不是分工原则：agent 还是那个 agent，
  「合入前 review、结果可复现」的要求一条不减。
  它真正解决的问题是把「人不退出流程」变得可持续——
  实验一跑几个小时，你不必守在那台机器前，但每个关键节点仍然经过你的眼睛。
]

== 验证原则

多 agent 提速之后，验证的原则反而要更硬，因为出错的通道变多了：

- agent 写的每一段实验代码，*合入前必须人工 review diff*——
  在 ikanban 的 Review flow 里做，或在终端里做，但不能不做。
  重点盯三处：数据有没有泄漏（测试集混进训练）、baseline 配置是否公平、
  指标计算和论文定义是否一致。
- 结果数字要能*从脚本一键复现*：每个实验任务的交付物必须包含复现命令，
  「agent 说结果是这样」不是结果。抽查一次：删掉输出目录，重跑，数字要对得上。
- 并行 agent 回收时，*先检查冲突再整合*：两个 agent 各自改了同一个配置文件，
  是并行工作流里最常见的静默错误。

这仍然是 @foundations:map 那张图的分工：AI 做加法——铺开实现、并行推进；
人做减法——review、复现、把关。实验阶段的「人 · 验证」一格，
内容就是上面这三条。

== 本章练习

#exercise[
  为你的贯穿示例设计并执行一个*最小实验*，完整走一遍本章流程：
  + *写一页纸任务书*：要验证什么（指回第 3 章选定的问题）、
    和谁比、怎么算成功；
  + *拆计划*：用 `writing-plans` skill（或 Claude Code plan mode）
    把任务书拆成小任务的计划文档，人工过一遍并修正至少一处问题——
    如果一处都挑不出来，多半是你没读；
  + *并行执行*：找出计划中至少两个互不依赖的任务，
    并行派发 subagent 完成，其余任务串行推进；
    过程用 ikanban（本地或远程模式）监督：审批、看 Task graph、在 Review flow 里审 diff；
  + *交付*：经过你逐行 review 的 diff，加上一个一键复现脚本——
    删掉输出重跑一次，确认数字一致后，这个实验才算「做完了」。
  实验产出的数据先留好：下一章把它们变成论文里的图和表。
]
